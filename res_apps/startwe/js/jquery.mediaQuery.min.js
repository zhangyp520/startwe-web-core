$define([], function(t) {
    return t.extend({
        mediaQuery: function(e) {
            var c, a = window.matchMedia("(min-width:1025px)"), i = window.matchMedia("(min-width:769px)"), o = window.matchMedia("(max-width:768px)"), d = t.extend({
                callback1024: function() {},
                callback7681024: function() {},
                callback768: function() {},
                keep: !1,
                node: !1,
                firstLoad: !0,
                once: !1
            }, e), n = 0;
            d.once && (n = 1);
            var r = function() {
                var e = document.documentElement.clientWidth;
                if (a.matches)
                    (c <= 1024 || e <= 1024 || d.firstLoad && e == c || d.keep) && (!d.once || d.once && 1 === n) && (d.callback1024(),
                    n = 0);
                else if (i.matches) {
                    (c <= 768 || 1024 < c || e <= 768 || 1024 < e || d.firstLoad && e == c || d.keep) && (!d.once || d.once && 1 === n) && (d.callback7681024(),
                    n = 0)
                } else if (o.matches) {
                    (768 < c || 768 < e || d.firstLoad && e == c || d.keep) && (!d.once || d.once && 1 === n) && (d.callback768(),
                    n = 0)
                }
                c = e
            };
            return function() {
                var e, n, t;
                if (c = document.documentElement.clientWidth,
                d.node) {
                    if (a.matches)
                        return 1024;
                    if (i.matches)
                        return 7681024;
                    if (o.matches)
                        return 768
                } else
                    r(),
                    e = window,
                    n = "resize",
                    t = function() {
                        r()
                    }
                    ,
                    e.addEventListener ? e.addEventListener(n, t, !1) : e.attachEvent ? e.attachEvent("on" + n, t) : e["on" + n] = t
            }()
        }
    }),
    t
});
